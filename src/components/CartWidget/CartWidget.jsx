import { FiShoppingCart } from "react-icons/fi";

const CartWidget = () => {
    return(
        <div>
            <FiShoppingCart className='carrito'/>
        </div>
    )
}

export default CartWidget